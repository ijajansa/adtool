@extends('layouts.app')
@section('title','Sync historical insights')
@section('content')
<div class="mx-auto" style="max-width:760px"><h1 class="h3 fw-bold">Sync historical insights</h1><p class="text-secondary">Queue up to {{ config('ads.insights.backfill_max_days') }} days. Large requests are split into bounded API chunks.</p><form class="card content-card" method="POST" action="{{ route('analytics.backfill.store') }}"><div class="card-body p-4">@csrf<div class="row g-3"><div class="col-md-6"><label class="form-label">Date from</label><input class="form-control" type="date" name="date_from" value="{{ old('date_from',now()->subDays(29)->toDateString()) }}" required></div><div class="col-md-6"><label class="form-label">Date to</label><input class="form-control" type="date" name="date_to" value="{{ old('date_to',now()->toDateString()) }}" required></div><div class="col-12"><label class="form-label">Campaign (optional)</label><select class="form-select" name="campaign_id"><option value="">All published campaigns</option>@foreach($campaigns as $campaign)<option value="{{ $campaign->id }}">{{ $campaign->name }}</option>@endforeach</select></div></div><button class="btn btn-primary mt-4">Queue synchronization</button></div></form></div>
@endsection
